<div align="center">

# ESP32 BLE Gateway

**企业级 CP02 充电站分布式监控系统**

[![License](https://img.shields.io/badge/License-MIT-blue.svg)](../../LICENSE)
[![ESP32](https://img.shields.io/badge/ESP32--S3-PlatformIO-E7352C.svg?logo=espressif&logoColor=white)](https://platformio.org/)
[![Python](https://img.shields.io/badge/Python-3.9+-3776AB.svg?logo=python&logoColor=white)](https://python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.100+-009688.svg?logo=fastapi&logoColor=white)](https://fastapi.tiangolo.com/)
[![Docker](https://img.shields.io/badge/Docker-Ready-2496ED.svg?logo=docker&logoColor=white)](https://docker.com/)

[English](#english) | [简体中文](#简体中文)

</div>

---

## 简体中文

### 📖 项目简介

**ESP32 BLE Gateway** 是一个企业级的分布式充电站监控解决方案，通过 ESP32-S3 作为 BLE 网关，实现对多个 CP02 充电站的实时监控、远程控制和数据采集。

### ✨ 核心特性

| 特性 | 描述 |
|------|------|
| 🔗 **多网关支持** | 同时管理多个 ESP32 网关，集群化监控 |
| 📡 **WebSocket 实时推送** | 毫秒级数据更新，无需轮询 |
| 💾 **历史数据持久化** | SQLite 存储，支持功率统计和趋势分析 |
| 🔐 **API 认证** | 可选 API Key 保护所有端点 |
| 📲 **WiFiManager** | 免编码配置 WiFi 和 MQTT，手机扫码即用 |
| 🚀 **OTA 更新** | 支持 ArduinoOTA 和 HTTP 固件升级 |
| 🐳 **Docker 部署** | 一键启动，生产环境就绪 |
| 📊 **现代化 UI** | 响应式设计，支持深色模式 |

### 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户层 (User Layer)                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │   浏览器     │  │   移动端     │  │   API 客户端 │              │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘              │
└─────────┼────────────────┼────────────────┼─────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      服务层 (Service Layer)                      │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              Python Backend (FastAPI) :5225                │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────────┐  │ │
│  │  │ REST API │ │WebSocket │ │历史存储   │ │ 网关管理       │  │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └───────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────┬───────────────────────────────────┘
                              │ MQTT
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      消息层 (Message Layer)                      │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                 MQTT Broker (Mosquitto) :1883              │ │
│  └────────────────────────────────────────────────────────────┘ │
└───────────┬─────────────────┬─────────────────┬─────────────────┘
            │                 │                 │
            ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                      网关层 (Gateway Layer)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ ESP32-S3 #1  │  │ ESP32-S3 #2  │  │ ESP32-S3 #N  │          │
│  │   (gw01)     │  │   (gw02)     │  │   (gwN)      │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
└─────────┼─────────────────┼─────────────────┼───────────────────┘
          │ BLE             │ BLE             │ BLE
          ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                      设备层 (Device Layer)                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  CP02 充电站  │  │  CP02 充电站  │  │  CP02 充电站  │          │
│  │   5 端口      │  │   5 端口      │  │   5 端口      │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

### 📂 项目结构

```
esp32-ble-gateway/
├── firmware/                    # ESP32-S3 固件 (PlatformIO)
│   ├── platformio.ini           # 编译配置
│   ├── include/
│   │   ├── config.h             # 设备配置
│   │   └── protocol.h           # CP02 BLE 协议
│   └── src/
│       ├── main.cpp             # 主程序 (36个命令处理器)
│       └── protocol.cpp         # 协议解析
│
├── backend/                     # Python 后端 (FastAPI)
│   ├── app.py                   # 主服务 (WebSocket + REST API)
│   ├── mqtt_client.py           # MQTT 客户端
│   ├── history_store.py         # 历史数据存储 (SQLite)
│   ├── requirements.txt         # Python 依赖
│   └── .env.example             # 环境变量模板
│
├── frontend/                    # Web 前端
│   ├── index.html               # 主页面
│   ├── script.js                # 前端逻辑
│   ├── styles.css               # 样式文件
│   └── *.png                    # 线材图标资源
│
├── docker/                      # Docker 部署
│   ├── docker-compose.yml       # 编排配置
│   ├── Dockerfile.backend       # 后端镜像
│   └── mosquitto/config/        # MQTT 配置
│
└── README.md                    # 本文件
```

### 🚀 快速开始

#### 方式一：Docker 部署（推荐生产环境）

```bash
# 1. 克隆项目
git clone https://github.com/poboll/cp02-ble.git
cd cp02-ble/software/esp32-ble-gateway

# 2. 启动服务
cd docker
docker-compose up -d

# 3. 访问
open http://localhost:5225
```

#### 方式二：本地开发

```bash
# 1. 安装 Python 依赖
cd backend
pip install -r requirements.txt

# 2. 启动后端
python app.py

# 3. 访问 http://localhost:5225
```

#### 方式三：ESP32 固件烧录

```bash
# 1. 安装 PlatformIO
pip install platformio

# 2. 编译并烧录
cd firmware
pio run -t upload -e esp32s3_serial

# 3. 首次启动后连接 WiFi 热点 "ESP32-BLE-Gateway"
# 4. 打开 http://192.168.4.1 配置 WiFi 和 MQTT
```

### 📡 API 接口

#### 网关管理

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/health` | GET | 健康检查 |
| `/api/gateways` | GET | 获取所有网关列表 |
| `/api/gateway/{id}` | GET | 获取网关详情 |
| `/api/gateway/{id}/ports` | GET | 获取端口状态 |

#### 设备控制

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/gateway/{id}/port/{p}/on` | GET | 开启端口 |
| `/api/gateway/{id}/port/{p}/off` | GET | 关闭端口 |
| `/api/gateway/{id}/reboot` | GET | 重启充电站 |
| `/api/gateway/{id}/cmd` | POST | 发送自定义命令 |

#### 历史数据

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/gateway/{id}/history` | GET | 查询端口历史数据 |
| `/api/gateway/{id}/stats` | GET | 功率统计 |
| `/api/gateway/{id}/hourly` | GET | 小时聚合数据 |
| `/api/gateway/{id}/events` | GET | 事件日志 |

#### WebSocket 实时推送

```javascript
// 连接 WebSocket
const ws = new WebSocket('ws://localhost:5225/ws');

ws.onmessage = (event) => {
    const data = JSON.parse(event.data);
    // data.type: 'init' | 'ports' | 'heartbeat' | 'timeout'
    // data.gateway_id: 网关 ID
    // data.data: 负载数据
};

// 订阅特定网关
ws.send(JSON.stringify({ type: 'subscribe', gateway_id: 'gw01' }));
```

### 🔧 支持的命令

ESP32 固件支持 36 个命令，涵盖：

| 类别 | 命令 |
|------|------|
| **设备管理** | `get_device_info`, `reboot`, `restart`, `factory_reset` |
| **端口控制** | `turn_on_port`, `turn_off_port`, `get_port_pd_status` |
| **显示设置** | `set_brightness`, `set_display_mode`, `flip_display` |
| **Token 管理** | `bruteforce_token`, `set_token` |
| **WiFi 管理** | `reset_wifi`, `get_wifi_status`, `scan_wifi` |
| **OTA 更新** | `ota_update`, `check_update` |

### ⚙️ 环境变量

| 变量 | 默认值 | 描述 |
|------|--------|------|
| `BLE_GW_MQTT_HOST` | localhost | MQTT 服务器地址 |
| `BLE_GW_MQTT_PORT` | 1883 | MQTT 端口 |
| `BLE_GW_SERVER_PORT` | 5225 | 后端服务端口 |
| `BLE_GW_API_KEY` | (空) | API 密钥，留空则不启用认证 |
| `BLE_GW_GATEWAY_TIMEOUT_SECONDS` | 30 | 网关超时时间（秒） |

### 📊 固件资源使用

| 资源 | 使用量 | 总量 | 占比 |
|------|--------|------|------|
| **RAM** | 57 KB | 320 KB | 17.4% |
| **Flash** | 1.12 MB | 3.34 MB | 33.5% |

### 🔄 重置 WiFi 设置

按住 ESP32 的 BOOT 按钮 (GPIO0) 5 秒钟，即可重置 WiFi 设置并进入配置门户。

---

## English

### 📖 Overview

**ESP32 BLE Gateway** is an enterprise-grade distributed charging station monitoring solution. It uses ESP32-S3 as BLE gateways to enable real-time monitoring, remote control, and data collection for multiple CP02 charging stations.

### ✨ Key Features

- 🔗 **Multi-Gateway Support** - Manage multiple ESP32 gateways simultaneously
- 📡 **WebSocket Real-time Push** - Millisecond-level data updates
- 💾 **Historical Data Persistence** - SQLite storage with power statistics
- 🔐 **API Authentication** - Optional API key protection
- 📲 **WiFiManager** - Code-free WiFi and MQTT configuration
- 🚀 **OTA Updates** - ArduinoOTA and HTTP firmware upgrades
- 🐳 **Docker Ready** - One-click deployment for production

### 🚀 Quick Start

```bash
# Docker deployment
cd docker && docker-compose up -d

# Local development
cd backend && pip install -r requirements.txt && python app.py

# ESP32 firmware
cd firmware && pio run -t upload -e esp32s3_serial
```

### 📡 API Endpoints

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/api/gateways` | GET | List all gateways |
| `/api/gateway/{id}/ports` | GET | Get port status |
| `/api/gateway/{id}/cmd` | POST | Send command |
| `/api/gateway/{id}/history` | GET | Query history data |
| `/ws` | WebSocket | Real-time updates |

---

## License

MIT License © 2023 在虎 (poboll)

See [LICENSE](../../LICENSE) for details.

---

<div align="center">

**Made with :heart: by [poboll](https://github.com/poboll)**

</div>
